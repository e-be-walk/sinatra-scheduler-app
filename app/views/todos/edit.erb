<!--- <h1>Edit your To Do List:</h1>
<br></br>
<form action="/todos %>" method="post">
  <h2>Select or Deselect from your present to-do list. Checked entries will remain on your list and unchecked items will be removed.</h2>
  <% @todo.each do |todo| %>
  <input type="radio" name="todo[user_id]" value="<%= user.id %>" <%= 'checked' if @user.todo.name == todo.name%>>
  <%= todo.name %></input>
  <% end %>
  <br></br>
  <h2>Or Create a New Entry for your To-Do List:</h2>
  <input type="text" name="todo[name]" id="todo_name"></input>
  <br></br>
  <input type="submit" name="submit" value="Submit Edits">
</form>

--->
